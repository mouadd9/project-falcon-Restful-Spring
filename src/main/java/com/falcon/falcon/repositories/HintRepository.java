package com.falcon.falcon.repositories;

import com.falcon.falcon.entities.Hint;
import org.springframework.data.jpa.repository.JpaRepository;

public interface HintRepository extends JpaRepository<Hint, Long> {
}
